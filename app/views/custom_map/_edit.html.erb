<%= form_for(@map) do |f| %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit "Name Your Map!" %>
  </div>

<% end %>
